<phone:PhoneApplicationPage
    x:Class="Kulka3.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="Score"/>

        <Grid x:Name="Grid" Grid.Row="1">
            <Canvas HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Ellipse x:Name="Ball" Canvas.Left="300" Canvas.Top="300" Width="40" Height="40" Fill="Red" Canvas.ZIndex="10"/>
                <Ellipse x:Name="Hole" Height="80" Width="80" Fill="Black" StrokeThickness="6" Stroke="White" Canvas.Left="156" Canvas.Top="72"/>
            </Canvas>
        </Grid>
        <StackPanel x:Name="Message" Grid.Row="1" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock x:Name="Text" />
            <TextBox x:Name="Nick"/>
            <Button Content="Ok" Click="ScoreConfirmed_Click"/>
        </StackPanel>
    </Grid>
</phone:PhoneApplicationPage>